cmake_minimum_required(VERSION 3.10)

project(LineIntersection LANGUAGES CXX)

set (CMAKE_CXX_STANDARD 20)
set (CMAKE_CXX_STANDARD_REQUIRED True)

set (CMAKE_MODULE_PATH ${CMAKE_CURRENT_LIST_DIR}/build ${CMAKE_MODULE_PATH})

option (BUILD_UNIT_TESTS "Build the unit tests project" ON)

include (setup)

message (STATUS "Configuring src")
add_subdirectory (src)

if (BUILD_UNIT_TESTS)
    enable_testing ()
    message (STATUS "Unit tests enabled")

    define_property (
        TARGET PROPERTY OUTPUT_XML
        BRIEF_DOCS "Google Test Output"
        FULL_DOCS "List of Test Report XML files generated by Google Test"
    )

    message (STATUS "Configuring Google Test")
    add_subdirectory (thirdparty/googletest)

    message (STATUS "Configuring test")
    add_subdirectory (test)
endif (BUILD_UNIT_TESTS)